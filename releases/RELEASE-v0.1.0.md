# ShadowCore v0.1.0

## Artifact attendu
- `shadowcore-0.1.0.jar`

## État de build dans cet environnement
La compilation automatique n'a pas pu être finalisée ici à cause d'un blocage réseau vers les dépôts Maven Fabric (erreur tunnel/proxy `403 Forbidden`).

## Build local recommandé
```bash
gradle wrapper
./gradlew clean build
```

Le JAR sera généré dans `build/libs/`.
